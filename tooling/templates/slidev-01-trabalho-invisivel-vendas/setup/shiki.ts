// Bloco de código sempre escuro (Figma 4:239), independente do esquema de cores do Slidev.
import { defineShikiSetup } from '@slidev/types'

export default defineShikiSetup(() => ({
  themes: { dark: 'vitesse-dark', light: 'vitesse-dark' },
}))
